import { createContext, useState } from "react";

export const NavbarActiveContext = createContext(null)
export const IsLikeContext = createContext(null)
export const BlogDataContext = createContext(null)
export const OverlayActive = createContext(false)
